---
# handlers file for conesphere.mail.spamassassin
- name: restart_spampd
  shell: |
    rcctl enable spampd
    rcctl restart spampd
  #service:
  #  name: spampd
  #  enabled: yes
  #  state: restarted
- name: restart_smtpd
  shell: |
    rcctl enable smtpd
    rcctl restart smtpd

